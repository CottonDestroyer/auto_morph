[package]
name = "auto_morph"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = { version = "0.31.1", default-features = true, features = [
    "default_fonts", # its alr default lol
    "wgpu",          # winner's gpu
] }
egui-file-dialog = "0.10.0"
enigo = "0.5.0"
rdev = "0.5.3"
